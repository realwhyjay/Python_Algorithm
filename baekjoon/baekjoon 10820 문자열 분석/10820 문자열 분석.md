# 백준 10820 : 문자열 분석

#### Date : 21/07/26

#### Lang : Python

#### Question : [백준 10820](https://www.acmicpc.net/problem/10820)

## 최종 답안 

```python

while True:
    try:
        str = list(input())
        low, upper, num, blank = 0, 0, 0, 0

        for i in range(len(str)):
            if str[i] == ' ':
                blank += 1
            elif ord('a') <= ord(str[i]) <= ord('z'):
                low += 1
            elif ord('A') <= ord(str[i]) <= ord('Z'):
                upper += 1
            elif 0 <= int(str[i]) <= 9:
                num += 1
        print(low, upper, num, blank)
    except EOFError:
        break

```

이상하게도 입력을 제한없이 받는(= 종료조건이 없는) 문제였다.   
그래서 실행하면 EOFError가 뜬다.  
EOF는 End of File을 의미하는데, 해당 코드는 무한 while문을 돌고있기 때문에   
언제 파일의 끝이 올지 예상할 수 가 없어 EOFError가 뜬 것이다.  
이를 해결해주기 위해 try except 문을 활용하여 예외처리를 해주었다.  

문제의 조건의 경우에는 , ord()를 사용해주어 해당 문자가 소문자인지 대문자인지를 구분해주고,   
int를 활용하여  숫자인지를 파악해주었다.  

## 모범 답안

```python

while True:
    try:
        str = list(input())
        low, upper, num, blank = 0, 0, 0, 0

        for i in range(len(str)):
            if str[i].isspace():
                blank += 1
            elif str[i].islower():
                low += 1
            elif str[i].isupper():
                upper += 1
            elif str[i].isdigit():
                num += 1
        print(low, upper, num, blank)
    except EOFError:
        break

```

사실 굳이 ord()를 사용해주거나 직접적으로 범위를 지정하여 확인을 해줄 필요가 없었다.  
파이썬에는 `isspace()`, `islower()`. `isupper()`, `isdigit()` 같이 문자열을 검사해주는 간편한 함수들이 많았다 ;;   
왜 굳이 어렵게 생각했니 바보같은 나야?

